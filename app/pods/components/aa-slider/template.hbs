<div class="wrap">
  <input
    type="range"
    mouseUp={{this.onSliderChanged}}
    min="{{min}}"
    max="{{max}}"
    step="{{step}}"
    value="{{value}}"
    style={{rangeStyle}}
  />
</div>

{{#if showValue}}
  <div class="value-container">
    <div class="value-text">
      {{valueTitle}}
    </div>
    <div class="value-separator"></div>
    <div class="value-text">{{value}}</div>
  </div>
{{/if}}
